name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build APK (Preview)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: artifacts/linea-blanca-pro

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: .

      - name: Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Build APK
        run: eas build --platform android --profile preview --non-interactive --output ../../../linea-blanca-pro.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: linea-blanca-pro-apk
          path: linea-blanca-pro.apk
          retention-days: 30
